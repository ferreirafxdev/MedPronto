port: 7880
bind_addresses:
  - ""
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: true
  # Configuração TURN integrada ou via Coturn externo
  turn_servers:
    - host: turn.seu-dominio.com
      port: 3478
      protocol: udp
      username: username
      password: password

keys:
  devkey: secret

logging:
  level: info
