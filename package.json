{
  "scripts": {
    "start": "cd backend && node dist/index.js",
    "build": "cd backend && npm install && npm run build"
  },
  "dependencies": {
    "socket.io": "^4.8.3"
  },
  "optionalDependencies": {
    "bufferutil": "*",
    "utf-8-validate": "*"
  }
}
